require "spec_helper"

describe Deck do
  describe "#build_deck" do
    it "builds a deck of 52 cards" do
    end

    it "creates unique cards" do
    end
  end

  describe "#cards" do
    it "passes" do
      expect(1).to eq(1)
    end
  end

  describe "#deal" do
    it "removes correct number of cards" do
    end

    it "deals the top cards in the deck" do
    end
  end

  describe "#hit" do
    it "deals the first card in the deck" do
    end
  end
end
